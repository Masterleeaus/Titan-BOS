# Worksuite Patch Plan — In-Place Edit

Module root: (this folder)
Detected module name: **Subcontractor**

Copy these into your module root (paths preserved). Then edit `module.json` to include the provider.

Follow the README from the previous message if needed.
